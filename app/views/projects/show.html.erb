<%= image_tag @project.main_image unless @project.main_image.nil? %>
<h1><%= @project.title %></h1>
<em><%= @project.subtitle %></em>
<p><%= @project.body %></p>
<%= link_to  "Edit", edit_project_path(@project) %>
